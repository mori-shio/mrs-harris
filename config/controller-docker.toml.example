# Mrs. Harris Controller 設定 (Docker Compose 環境用)
#
# このファイルをコピーして controller-docker.toml として使用してください:
#   cp config/controller-docker.toml.example config/controller-docker.toml

[server]
host = "0.0.0.0"
port = 8080
external_url = "http://web:8080"

[database]
url = "mysql://mrs_harris:YOUR_DB_PASSWORD@mysql:3306/mrs_harris"
max_connections = 20

[scheduler]
poll_interval_sec = 5
reaper_interval_sec = 30
worker_timeout_sec = 3600

[fargate]
cluster_arn = "arn:aws:ecs:ap-northeast-1:YOUR_ACCOUNT_ID:cluster/mrs-harris"
task_definition = "mrs-harris-worker:1"
subnets = ["subnet-xxxxxxxx"]
security_groups = ["sg-xxxxxxxx"]
container_name = "mrs-harris-worker"

[lambda]
function_name = "mrs-harris-worker"

[auth]
jwt_secret = "REPLACE_WITH_A_LONG_RANDOM_STRING"
jwt_expiry_hours = 24

[notification]

[notification.slack]
default_webhook_url = ""

[notification.email]
smtp_host = "smtp.example.com"
smtp_port = 587
from_address = "mrs-harris@example.com"
